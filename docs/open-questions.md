# Спорные решения спринта 3

Зафиксированные решения по PLAN-5. Помечено, что решено сейчас и что стоит
пересмотреть при реальной нагрузке.

## Embedding-модель и размерность векторов

**Решено:** `text-embedding-3-small` (OpenAI), 1536 измерений
(`config/embedding.config.js`, `EMBEDDING_MODEL`/`EMBEDDING_DIM`). Дёшево,
`OPENAI_API_KEY` уже сконфигурирован в проекте. Смена модели требует
пересоздания коллекции Qdrant (размерность зафиксирована при создании) —
если модель поменяется, нужен `docs/expert-context.md` апдейт + reindex всего
проектного контекста.

## Транспорт очереди обработки файлов

**Решено:** MongoDB-backed очередь (`src/models/job.model.js`,
`src/services/queue/`), а не BullMQ + Redis. Причина: деплой сейчас — один
Docker-контейнер без Redis (`amvera.yml`), а обязательный по ТЗ объём
(TXT/MD) — не CPU-тяжёлый. Контракт `enqueue/registerHandler` намеренно
узкий, чтобы транспорт можно было заменить на BullMQ, не трогая обработчики
(`process-file.job.js` и будущие job-типы).

**Пересмотреть, если:** появится реальная нагрузка с большим количеством
PDF/DOCX или нужны priority queues / rate limiting из коробки.

## Какие форматы файлов реально парсятся

**Решено:** TXT/MD — обязательно, потоково (`text.extractor.js`), без
буферизации всего файла. PDF (`pdf-parse`) и DOCX (`mammoth`) — желательно,
буферизуются целиком с лимитом 20 МБ (`MAX_BYTES` в
`pdf.extractor.js`/`docx.extractor.js`). PPTX/XLSX/изображения — не
поддерживаются, файл получает `processingStatus=unsupported`, загрузка не
ломается.

## Хранение текста чанка в Qdrant payload

**Решено:** да, `payload.text` хранит сам текст фрагмента (не только
`sourceId` для последующего восстановления из MongoDB). Так сильно проще
собирать LLM-контекст без похода в MongoDB за каждым результатом поиска.
Секретов там нет — только проектный контент (файлы, summary, артефакты),
что не противоречит SEC-3.

**Пересмотреть, если:** появится требование не дублировать контент между
MongoDB и Qdrant (например, право на удаление/забвение конкретного текста).

## Способ завершения агента / создания артефакта

**Решено:** двухфазное завершение через `ExpertSession.status`:
`waiting_user_confirmation` (черновик, `Artifact.status=ready`) →
`completed` (`confirmArtifact:true`, `Artifact.status=confirmed`). Артефакт
генерируется отдельным LLM-вызовом со строгой инструкцией вернуть JSON, а не
парсингом последнего сообщения чата — это надёжнее и проще валидировать.

## Хранение версий системного промпта

**Не реализовано** (`AgentPromptVersion` — в ТЗ как STRETCH-3,
«опционально»). Сейчас `Agent.systemPrompt` — просто поле, без истории
версий. `ExpertSession.inputContextSnapshot` частично компенсирует это для
аудита — там виден фактический промпт на момент конкретного запроса.

## Лимиты токенов

**Решено:** ограничение контекста — по символам (`maxContextChars`), не по
токенам, поскольку в проекте нет токенизатора конкретной модели, а
символьный лимит — специально разрешённая ТЗ альтернатива
(`maxContextChars` наравне с `maxTokens`). `Agent.modelConfig.maxTokens`
используется только как параметр LLM-вызова (лимит на *ответ*, не на
контекст).

## Генерация кода агента (R1/R2 и т.д.)

**Решено:** `Agent.code` — свободная строка, не enum. Роман/Регина заведены
только как демо-данные (`scripts/seed-demo-agents.js`), не как часть
бизнес-логики — систему администратор наполняет агентами сам через
`POST /accelerator/admin/agents`.

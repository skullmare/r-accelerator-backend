import openAiClient from '../../../config/openai.config.js'
import logger from '../../../config/logger.config';

async function sendMessageAssistant (messageText, assistantId, threadId = null) {
    
}